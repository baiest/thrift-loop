# 014 — Tasks

- [x] Write failing tests for `PhotoCarousel` (no photos, single photo, multi-photo nav, wrap-around, dot jump, error fallback)
- [x] Implement `PhotoCarousel` to make the tests pass
- [x] Wire `PhotoCarousel` into `auction-detail-page.tsx`, removing `AuctionPhoto` and `photoFailed` state
- [x] Re-run `auction-detail-page.test.tsx` to confirm existing placeholder/error-fallback behavior still holds
- [x] Lint clean (`eslint --max-warnings=0`) on all changed files
- [x] Full web test suite green (505/505)
- [x] `npm run build` clean across api/web/shared
- [x] Write this spec/plan/tasks retroactively (process was skipped — see spec.md note)
